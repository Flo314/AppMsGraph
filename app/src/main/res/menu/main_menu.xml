<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_filtre"
        app:showAsAction="ifRoom"
        android:orderInCategory="1"
        android:icon="@drawable/ic_filter_24dp"
        android:title="@string/filter">

        <menu>
            <item
                android:id="@+id/important"
                android:title="@string/important"
                android:icon="@drawable/ic_access_red"
                />
            <item
                android:id="@+id/moyen"
                android:title="@string/moyen"
                android:icon="@drawable/ic_access_orange"
                />
            <item
                android:id="@+id/bon"
                android:title="@string/bon"
                android:icon="@drawable/ic_access_green"
                />
        </menu>

    </item>

    <item
        android:id="@+id/action_search"
        app:showAsAction="ifRoom|collapseActionView"
        android:orderInCategory="2"
        android:icon="@drawable/ic_search_24dp"
        android:title="@string/search"
        app:actionViewClass="android.support.v7.widget.SearchView" />

</menu>